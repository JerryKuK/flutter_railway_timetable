# common Specification

## Purpose
程式碼遵循clean code的原則
開發時使用superpowers的TDD來開發
依賴注入使用get_it `https://pub.dev/packages/get_it`
物件用freezed `https://pub.dev/packages/freezed`
當我使用這個指令 /opsx:verify 確認完後, 再用 Agent Device 開啟ios模擬器幫我錄製這次的多個需求測試，存到 ./workflows/`$需求`.ad